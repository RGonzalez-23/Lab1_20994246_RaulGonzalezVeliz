#lang scheme






; Descripción: Función que genera una ficha de conecta 4.
; Dom: color (string).
; Rec: ficha (piece). 
; Tipo recursión: No aplica.

(define (piece color)
  (string-downcase color)
  )
  
